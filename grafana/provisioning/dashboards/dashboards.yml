# Grafana Dashboard Provisioning
# Auto-loads dashboards from /var/lib/grafana/dashboards on startup

apiVersion: 1

providers:
  - name: 'MediAssist Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
